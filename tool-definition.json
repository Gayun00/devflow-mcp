{
  "tools": [
    {
      "name": "github/findOrCreatePr",
      "description": "이슈 번호와 병합 대상 브랜치를 기반으로 PR을 생성하거나 업데이트합니다.",
      "input_schema": {
        "type": "object",
        "properties": {
          "issueId": { "type": "string" },
          "base": { "type": "string" }
        },
        "required": ["issueId", "base"]
      }
    }
  ]
}
